spring:
    application:
        name: user-service
    profiles:
        active: test
    datasource:
        name: pool-to-user-service
        username: postgres
        password: password
        url: jdbc:postgresql://127.0.0.1:5432/user_service
    auth:
        keystore:
            path: classpath:keystore.jks
            secret: password
            key-alias: io.electrica


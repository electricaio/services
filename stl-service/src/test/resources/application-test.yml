spring:
    profiles:
        active: test
    application:
        name: stl-service
    datasource:
        type: com.zaxxer.hikari.HikariDataSource
        url: jdbc:postgresql://localhost:5432/stl
        name: stl-ds
        username: postgres
        password: ""